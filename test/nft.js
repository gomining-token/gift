const truffleAssert = require("truffle-assertions");
const web3 = require("web3");

const GiftNftCollection = artifacts.require("GiftNftCollection");

contract("GiftNftCollection", (accounts) => {
  const owner = accounts[0];
});
